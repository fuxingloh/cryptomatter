{
  "$schema": "https://turborepo.org/schema.json",
  "extends": ["//"],
  "pipeline": {
    "build": {
      "inputs": ["contented.config.mjs", "frontmatter/**"],
      "outputs": ["_*/", "index.js", "index.d.ts", "index.json"],
      "dependsOn": ["^build"]
    }
  }
}
